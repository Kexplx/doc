`ng serve --host 0.0.0.0 --allowed-hosts=true`
ssh -R 80:localhost:4200 ssh.localhost.run


---
or configure wsl to forward like this:

code %UserProfile%/.wslconfig
```
[wsl2]
networkingMode=mirrored
```
wsl --shutdown
