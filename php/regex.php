    // Returns 1 if matches were found, 0 if not
    $result = preg_match('/^\.+$/', "..3..");

     // Returns 1 if matches were found, 0 if not
     // Saves found matches in $matches[]
    $result = preg_match('/^\.+$/', "..3..", $matches[]);